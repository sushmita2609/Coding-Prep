void binSort(int Arr[], int n)          //Arr[] is given array and n is size of the array 
{
   
    int temp = -1;
    for(int i = 0; i < n; i++)
    {
       if(Arr[i] == 0)
       {
         temp++;
         swap(Arr[temp], Arr[i]);
       }
    }
}
